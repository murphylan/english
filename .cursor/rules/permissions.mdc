---
description: 权限系统规范 - 适用于权限检查、路由守卫、按钮保护相关代码
globs:
  - lib/permissions.ts
  - hooks/use-permission.ts
  - components/layout/permission-guard.tsx
  - constants/nav.ts
  - server/actions/**/*.ts
  - app/**/*.tsx
---

# 权限系统规范

## 架构概览

权限系统采用三层防护：

```
┌─────────────────────────────────────────────┐
│ 1. 侧边栏 + 命令面板：按权限过滤导航项       │
├─────────────────────────────────────────────┤
│ 2. PermissionGuard：路由级守卫，无权限显示403  │
├─────────────────────────────────────────────┤
│ 3. 按钮级 disabled + Server Action 权限校验  │
└─────────────────────────────────────────────┘
```

## 核心文件

| 文件 | 职责 |
|------|------|
| `lib/permissions.ts` | 权限常量 `PERMISSIONS`、内置角色种子数据、列表工具函数（`hasAnyPermission`、`canModifyOwnedResourceFromList` 等）；同步 `hasPermission(role, …)` 仅作兜底/兼容，**服务端鉴权勿依赖它** |
| `server/db/schema/role.ts` | `role` / `role_permission` 表（PostgreSQL 持久化角色与权限） |
| `server/role-permissions-service.ts` | `ensureBuiltinRolesSeeded`、`resolveRolePermissions`、`userHasPermissionAsync` |
| `server/role-permissions-cache.ts` | 角色权限解析结果短 TTL 内存缓存；写角色权限后需 `invalidate` |
| `server/actions/rolePermissionAction.ts` | `getMyPermissions`、`listRolesForAdmin`、`updateRolePermissions` 等 |
| `hooks/use-permission.ts` | 客户端：`getMyPermissions` + React Query，加载前用静态表作 `placeholderData` |
| `components/layout/permission-guard.tsx` | 页面级路由守卫（403） |
| `constants/nav.ts` | 导航项定义（含 `requiredPermissions`） |
| `server/auth/index.ts` | JWT/Session 中携带 `role`（角色**键**，与 `role.key` 一致） |

## 数据源说明（重要）

- **运行时权威**：某角色拥有的权限 ID 列表以数据库表 `role_permission` 为准（经 `resolveRolePermissions` / `userHasPermissionAsync` 读取）。
- **首次种子**：空库时 `ensureBuiltinRolesSeeded()` 会写入内置角色行及初始权限行（来源为 `lib/permissions` 中的内置角色种子 + 静态矩阵）；之后管理员在「角色管理」中的变更即持久化结果。
- **客户端**：`usePermission` / `useUserPermissions` 通过 `getMyPermissions` Server Action 拉取当前用户权限列表；未返回前可用静态表占位，**不得以 session.role 直接推断完整权限**。

## 权限常量（`lib/permissions.ts`）

使用 `PERMISSIONS` 对象统一管理权限 ID，禁止使用字符串字面量：

```typescript
import { PERMISSIONS } from "@/lib/permissions";

// ✅ 正确：导航 / 配置使用常量
requiredPermissions: [PERMISSIONS.userWrite]

// ✅ 正确：客户端组件内（来自 Hook 的回调）
const { hasPermission } = usePermission();
hasPermission(PERMISSIONS.settingsBackup);

// ❌ 错误：使用字符串字面量
requiredPermissions: ["user:write"]

// ❌ 错误：在 Client Component 里用 session.role + 同步 hasPermission(role, …) 当权威来源
```

## 角色权限矩阵

以下为**内置角色默认种子**参考；自定义角色及管理员在 UI 中的调整以数据库为准。

| 权限 | admin | pm | developer | user |
|------|-------|-----|-------------|------|
| 仪表盘 | ✅ | ✅ | ✅ | ✅ |
| 文档查看 | ✅ | ✅ | ✅ | ✅ |
| 文档新建 | ✅ | ✅ | ✅ | ❌ |
| 文档编辑（本人） | ✅ | ✅ | ✅ | ❌ |
| 文档编辑（任意） | ✅ | ✅ | ✅ | ❌ |
| 文档删除（本人） | ✅ | ✅ | ✅ | ❌ |
| 文档删除（任意） | ✅ | ✅ | ✅ | ❌ |
| 任务查看/编辑/删除 | ✅ | ✅ | 查看+编辑 | 查看 |
| PlantUML：查看 / 新建 / 编辑(本人) / 删除(本人) | ✅ | ✅ | ✅ | ✅（无编辑/删他人） |
| PlantUML：编辑(任意) / 删除(任意) | ✅ | ✅ | ✅ | ❌ |
| 白板：查看 / 新建 / 编辑(本人) / 删除(本人) | ✅ | ✅ | ✅ | ✅（无编辑/删他人） |
| 白板：编辑(任意) / 删除(任意) | ✅ | ✅ | ✅ | ❌ |
| 用户查看 | ✅ | ✅ | ✅ | ✅ |
| 用户管理 | ✅ | ❌ | ❌ | ❌ |
| 角色查看 | ✅ | ✅ | ✅ | ✅ |
| 角色管理 | ✅ | ❌ | ❌ | ❌ |
| 日志查看 | ✅ | ✅ | ✅ | ✅ |
| 操作日志（清除等） | ✅ | ✅ | ❌ | ❌ |
| 个人资料 | ✅ | ✅ | ✅ | ✅ |
| 数据库备份 | ✅ | ❌ | ❌ | ❌ |

## 客户端权限检查

### 方式一：`usePermission()`（推荐，提供便捷布尔值）

```tsx
import { usePermission } from "@/hooks/use-permission";
import { PERMISSIONS } from "@/lib/permissions";

export default function SomePage() {
  const { hasPermission, canManageUsers, canManageRoles } = usePermission();

  const canBackup = hasPermission(PERMISSIONS.settingsBackup);

  return (
    <Button disabled={!canManageUsers}>
      新增用户
    </Button>
  );
}
```

### 方式二：`useUserPermissions()`（返回 `string[]`，用于过滤逻辑）

```tsx
import { useUserPermissions } from "@/hooks/use-permission";
import { hasAnyPermission } from "@/lib/permissions";

const permissions = useUserPermissions();
const canView = hasAnyPermission(permissions, [PERMISSIONS.docRead]);
```

### 本人资源 vs 任意资源

涉及「仅本人可改删、具备 `*Any` 可操作他人资源」时，在客户端用权限列表 + `canModifyOwnedResourceFromList`（见 `lib/permissions.ts`），与 Server Action 侧规则保持一致。

## 按钮权限保护模式

### 模式一：disabled + Tooltip 提示（推荐）

按钮可见但不可点击，hover 时提示缺少的权限：

```tsx
import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from "@/components/ui/tooltip";

<TooltipProvider delayDuration={200}>
  <Tooltip>
    <TooltipTrigger asChild>
      <span className="inline-block">
        <Button
          onClick={handleAction}
          disabled={!canManageRoles}
        >
          <Plus className="mr-2 h-4 w-4" />
          新增角色
        </Button>
      </span>
    </TooltipTrigger>
    {!canManageRoles && (
      <TooltipContent>
        <p>需要「角色管理」权限</p>
      </TooltipContent>
    )}
  </Tooltip>
</TooltipProvider>
```

**注意：** `<Button disabled>` 不触发 hover 事件，所以需要用 `<span>` 包裹作为 `TooltipTrigger`。

### 模式二：条件渲染（敏感功能）

整个功能区块对无权限用户完全隐藏：

```tsx
{canBackup && (
  <TabsTrigger value="database">
    <Database className="mr-1.5 size-4" />
    数据库管理
  </TabsTrigger>
)}
```

### 模式三：DropdownMenuItem disabled

```tsx
<DropdownMenuItem
  onClick={() => onEdit(user)}
  disabled={!canManageUsers}
>
  <Edit className="mr-2 h-4 w-4" />
  编辑
  {!canManageUsers && (
    <span className="ml-auto text-[10px] text-muted-foreground">
      无权限
    </span>
  )}
</DropdownMenuItem>
```

## 导航项权限配置

每个导航项必须声明 `requiredPermissions`：

```typescript
// constants/nav.ts
import { PERMISSIONS } from "@/lib/permissions";

export interface NavItem {
  title: string;
  url: string;
  icon?: LucideIcon;
  items?: NavItem[];
  requiredPermissions?: string[];
}

export const navItems: NavItem[] = [
  {
    title: "用户",
    url: "/users",
    icon: Users,
    requiredPermissions: [PERMISSIONS.userWrite],
  },
];
```

侧边栏和命令面板自动根据权限过滤：

```tsx
// app-sidebar.tsx
const userPermissions = useUserPermissions();
const visibleNavItems = React.useMemo(
  () => filterNavigation(navItems, userPermissions),
  [userPermissions]
);
```

## PermissionGuard 路由守卫

`PermissionGuard` 包裹在 `app/(platform)/layout.tsx` 的 `<main>` 内，自动拦截无权限访问：

- 从导航配置提取 `path → requiredPermissions` 映射
- 支持精确匹配和前缀匹配（子路由继承父权限）
- 权限加载中先渲染 children，避免 403 闪烁
- 无权限显示 403 页面 + 返回仪表盘按钮

```tsx
// app/(platform)/layout.tsx
<main>
  <PermissionGuard>{children}</PermissionGuard>
</main>
```

## Server Action 权限校验（强制）

所有写操作（及需要按权限拒绝的读操作）的 Server Action **必须**在服务端校验权限；**使用数据库解析的异步 API**，禁止仅用同步 `hasPermission(session.user.role, …)` 作为唯一依据（静态表与 DB 可能不一致）。

```typescript
import { auth } from "@/server/auth";
import { PERMISSIONS } from "@/lib/permissions";
import { userHasPermissionAsync } from "@/server/role-permissions-service";

export async function deleteEntity(id: string) {
  const session = await auth();
  if (!session?.user?.id) {
    return { success: false, error: "未登录" };
  }
  const roleKey = session.user.role ?? "user";
  if (!(await userHasPermissionAsync(roleKey, PERMISSIONS.docDeleteAny))) {
    return { success: false, error: "没有权限执行此操作" };
  }
  // ...业务逻辑
}
```

`userHasPermissionAsync` 内部会 `ensureBuiltinRolesSeeded` 并走 `resolveRolePermissions`（含缓存）。修改角色权限的 Action 在写入后应调用 `invalidateRolePermissionsCache`。

### 特殊场景：用户可修改自己的数据

```typescript
import { userHasPermissionAsync } from "@/server/role-permissions-service";

const isSelf = session.user.id === userId;
if (
  !isSelf &&
  !(await userHasPermissionAsync(
    session.user.role ?? "user",
    PERMISSIONS.userWrite
  ))
) {
  return { success: false, error: "没有权限执行此操作" };
}
```

## Session 中的 role

`role` 字段在登录时写入 JWT，通过 session 传递到客户端：

- `session.user.role` — 当前用户角色（`"admin"` | `"pm"` | `"developer"` | `"user"`）
- 默认值为 `"user"`
- 类型定义在 `types/next-auth.d.ts`

## 检查清单

### 新增页面
- [ ] 导航项配置了 `requiredPermissions`
- [ ] 命令面板条目配置了 `requiredPermissions`

### 页面内操作按钮
- [ ] 写操作按钮使用 `disabled={!canXxx}` 保护
- [ ] disabled 按钮有 Tooltip 或 title 提示缺少的权限
- [ ] 敏感功能区块使用条件渲染隐藏

### Server Action
- [ ] 写操作（及需授权的读操作）使用 `userHasPermissionAsync(roleKey, PERMISSIONS.xxx)`（或等价地先 `resolveRolePermissions` 再判断）校验
- [ ] 权限不足返回 `{ success: false, error: "没有权限执行此操作" }`
- [ ] 变更角色权限后刷新缓存并视需要 `invalidateQueries` 客户端 `my-permissions`

## 禁止事项

- ❌ 权限 ID 使用字符串字面量（必须用 `PERMISSIONS.xxx` 常量）
- ❌ 只做前端权限检查不做 Server Action 校验（可被绕过）
- ❌ 只做 Server Action 校验不做前端保护（用户体验差）
- ❌ 无权限时隐藏按钮但仍可通过 URL 访问页面（需配合 `PermissionGuard`）
- ❌ 写操作的 Server Action 不检查权限（安全漏洞）
- ❌ 服务端仅用同步 `hasPermission(session.user.role, …)` 做唯一鉴权（绕过 DB 中的角色配置）
- ❌ 在 `PermissionGuard` 加载中显示 403（会闪烁，应先渲染 children）
